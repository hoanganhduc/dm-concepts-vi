name: Build and deploy book to gh-pages

on:
  push:
    branches: [main]
  workflow_dispatch:

# Deploying to gh-pages and creating releases both need write access.
permissions:
  contents: write

concurrency:
  group: dm-concepts-build
  cancel-in-progress: false

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install PreTeXt + deps
        run: pip install pretext pyyaml

      - name: Install Vietnamese locale
        # PreTeXt ships no vi locale; install ours so structural labels render
        # in Vietnamese. The script fetches PreTeXt's core on first run.
        run: bash workflow/scripts/install-vi-locale.sh

      - name: Validate entry data (build-gate)
        run: python3 workflow/scripts/validate-entry.py

      - name: Build web target
        run: pretext build web

      - name: Build quick-lookup search index
        # entries.json is gitignored, so regenerate it from data/terms/*.yaml.
        run: python3 workflow/scripts/build-search-index.py

      - name: Bundle quick-lookup page into the site
        run: |
          cp assets/search/tra-cuu.html assets/search/lookup.js assets/search/entries.json output/web/
          # Stop GitHub Pages' Jekyll from dropping PreTeXt's _static/ directory.
          touch output/web/.nojekyll

      - name: Deploy to gh-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./output/web
          publish_branch: gh-pages
          force_orphan: true

  # Build the print PDF and publish it as the latest GitHub Release. Runs only
  # after the web build/validation succeeds. Release tag (= the PDF's version) is
  # yyyy.mm.dd.N, where the date is the build (last-updated) date and N is the
  # release count for that day, starting at 1.
  release-pdf:
    needs: build-deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install PreTeXt + deps
        run: pip install pretext pyyaml

      - name: Install TeX Live (XeLaTeX, fonts, polyglossia)
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            texlive-xetex texlive-latex-recommended texlive-latex-extra \
            texlive-pictures texlive-fonts-recommended texlive-fonts-extra \
            texlive-lang-other latexmk

      - name: Install Vietnamese locale (also fetches PreTeXt core)
        run: bash workflow/scripts/install-vi-locale.sh

      - name: Build PDF (cover embedded, cross-ref links preserved)
        run: bash workflow/scripts/build-print.sh

      - name: Release latest PDF as yyyy.mm.dd.N
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          DATE=$(date +%Y.%m.%d)
          # How many releases already exist for today? Next number = count + 1.
          COUNT=$(gh release list --limit 300 --json tagName \
            --jq "[.[] | select(.tagName | startswith(\"${DATE}.\"))] | length")
          NUM=$((COUNT + 1))
          TAG="${DATE}.${NUM}"
          ASSET="Mot-so-thuat-ngu-Toan-roi-rac-va-KHMT-${TAG}.pdf"
          cp output/print/main.pdf "$ASSET"
          gh release create "$TAG" "$ASSET" \
            --title "PDF $TAG" \
            --notes "Bản PDF mới nhất, cập nhật ngày ${DATE} (phiên bản thứ ${NUM} trong ngày). Tạo tự động từ commit ${GITHUB_SHA}."
